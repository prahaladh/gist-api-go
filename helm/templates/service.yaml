apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-gist-api.fullname" . }}
  labels:
    {{- include "go-gist-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "go-gist-api.selectorLabels" . | nindent 4 }}
